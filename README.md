# windows-ansible
Powershell Script to ensure Ansible playbooks can run on Windows target hosts. This is for a cyber competition to make it easy to make Windows machines reachable by playbooks.

```sh
Set-ExecutionPolicy Bypass -Scope Process -Force
.\winrm.ps1
```
